# filesystem/

Product docs live in the main project [README.md](../README.md) under the spoiler **filesystem/ (product modules)**.

This folder stays editable next to the frozen binary (no package `__init__.py`). Import `filesystem.api` / `filesystem.tool_arg` after worker `base_dir` is on `sys.path`.
